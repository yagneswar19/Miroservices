# ===============================
# Server
# ===============================
server.port=8086

# ===============================
# Application Name (Eureka display)
# ===============================
spring.application.name=API-GATEWAY

# ===============================
# Eureka Client Configuration
# ===============================
eureka.client.service-url.defaultZone=http://localhost:8761/eureka/
eureka.client.register-with-eureka=true
eureka.client.fetch-registry=true

# ===============================
# Gateway Configuration
# ===============================
# Enable service discovery (optional – auto routes for all services)
spring.cloud.gateway.discovery.locator.enabled=true
spring.cloud.gateway.discovery.locator.lower-case-service-id=true

# ✅ CORS is handled by CorsConfig.java (Java-based configuration)
# Removed property-based CORS to avoid conflicts

# ===============================
# CUSTOMER SERVICE
# ===============================
spring.cloud.gateway.routes[0].id=CustomerMs
spring.cloud.gateway.routes[0].uri=lb://CustomerMs
spring.cloud.gateway.routes[0].predicates[0]=Path=/api/customers/**

# ===============================
# Swagger / OpenAPI
# ===============================
springdoc.swagger-ui.path=/swagger-ui.html
springdoc.api-docs.path=/v3/api-docs

# ===============================
# Actuator (optional)
# ===============================
management.endpoints.web.exposure.include=*
